version: '3.8'

services:
  postgres:
    container_name: transaction-challenges
    image: postgres
    ports:
    - 5432:5432
    environment:
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
      - POSTGRES_DB=